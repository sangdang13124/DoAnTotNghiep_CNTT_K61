/// Create Event for obj_tree
health1 = 10;

image_speed = 0;
// obj_tree Create Event

// Thiết lập depth trong sự kiện `Create` của `obj_tree`
depth = -y; // Giá trị depth âm để đảm bảo các đối tượng với y lớn hơn (phía dưới) được vẽ sau
