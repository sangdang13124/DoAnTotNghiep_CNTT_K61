if (place_meeting(x, y, obj_player)) {
    // Tạo một instance mới của Object42 nếu nó chưa tồn tại

    
    // Di chuyển obj_player đến tọa độ đích
    obj_player.x = target_x;
    obj_player.y = target_y;
}